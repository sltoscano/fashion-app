notes:
	Folder for storing random notes and images for the project.

outfit-viewer:
	The initial open-gl viewer of outfits from retailer websites. Reads in metadata generated by the screen-scrapers. The open-gl portion is a 3d cube that displays front, side, side, back images of each outfit.

screen-scrapers:
	Scripts that parse the retailer websites for images to download. These scripts also generate metadata for each outfit downloaded. The metadata is to be used by the view to display information such as price, product id, store, etc.
